/*
 * Les MEMES valeurs que `--cx-radius-*` du web — un test compare les deux fichiers.
 * Elles ont grandi avec le passage au verre : un angle vif trahit une matiere epaisse.
 */
export const radius = { none: 0, sm: 12, md: 18, lg: 24, xl: 32, pill: 999 } as const;
