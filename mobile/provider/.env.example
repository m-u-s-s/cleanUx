# Required for API calls
# Note: Provider app uses the same API backend as the client — the server
# determines role from the authenticated user record, not the endpoint.
EXPO_PUBLIC_API_URL=http://localhost:8000/api

# Optional — Sentry crash reporting (disabled if empty)
EXPO_PUBLIC_SENTRY_DSN=
