// Re-export from shared so both apps share one implementation.
export { offlineQueue } from '../../../shared/src/lib/offlineQueue';
export type { QueuedAction } from '../../../shared/src/lib/offlineQueue';
