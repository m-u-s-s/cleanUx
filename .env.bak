APP_NAME=CleanUx
APP_ENV=local
APP_KEY=base64:JcR2PpJITotOBmzP4Ub9lRqBWWP6siEZbVumIhtlVcA=
APP_DEBUG=true
APP_URL=http://localhost

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=cleanux
DB_USERNAME=root
DB_PASSWORD=root

BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=database
SESSION_LIFETIME=120

MEMCACHED_HOST=127.0.0.1

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_HOST=127.0.0.1
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="test@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

STRIPE_KEY=pk_test_xxx
STRIPE_SECRET=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
STRIPE_PREMIUM_PRICE_ID=price_xxx
STRIPE_CONNECT_REFRESH_URL="${APP_URL}/dashboard/employe/stripe-connect/refresh"
STRIPE_CONNECT_RETURN_URL="${APP_URL}/dashboard/employe/stripe-connect/return"
CLEANUX_PLATFORM_FEE_PERCENT=20

CASHIER_CURRENCY=eur
CASHIER_CURRENCY_LOCALE=fr_BE


CLEANUX_EMPLOYEE_HOURLY_COST=18
CLEANUX_TRAVEL_COST=8
CLEANUX_MATERIAL_COST_RATE=0.08

GOOGLE_MAPS_API_KEY=ta_cle_google_maps
VITE_GOOGLE_MAPS_API_KEY="${GOOGLE_MAPS_API_KEY}"


TWILIO_SID=xxxx
TWILIO_TOKEN=xxxx
TWILIO_FROM=+32xxxxxxxx

BROADCAST_CONNECTION=reverb

REVERB_APP_ID=cleanux-local
REVERB_APP_KEY=cleanux-local-key
REVERB_APP_SECRET=cleanux-local-secret
REVERB_HOST=127.0.0.1
REVERB_PORT=8080
REVERB_SCHEME=http

VITE_REVERB_APP_KEY="${REVERB_APP_KEY}"
VITE_REVERB_HOST="${REVERB_HOST}"
VITE_REVERB_PORT="${REVERB_PORT}"
VITE_REVERB_SCHEME="${REVERB_SCHEME}"

OPS_MONITORING_NOTIFY_EMAIL=mm.darouch@hotmail.com